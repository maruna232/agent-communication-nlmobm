# 
# This is an example configuration file. Create a copy named .env.local with your actual values.
# Do not commit .env.local to version control.
# All variables prefixed with NEXT_PUBLIC_ will be exposed to the browser.

# Firebase Authentication Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=Your Firebase API key from Firebase console
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id

# OpenAI API Configuration
NEXT_PUBLIC_OPENAI_API_KEY=Your OpenAI API key for GPT-4o access

# Google Calendar API Configuration
NEXT_PUBLIC_GOOGLE_API_KEY=Your Google API key for Calendar API
NEXT_PUBLIC_GOOGLE_CLIENT_ID=Your Google OAuth client ID for Calendar integration
NEXT_PUBLIC_GOOGLE_CLIENT_SECRET=Your Google OAuth client secret for Calendar integration

# WebSocket Server Configuration
NEXT_PUBLIC_WEBSOCKET_URL=wss://api.aiagentnetwork.com or wss://localhost:8080 for local development

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000 for local development or your production URL
NEXT_PUBLIC_API_URL=http://localhost:8080/api for local development or your production API URL

# Feature Flags
NEXT_PUBLIC_ENABLE_CALENDAR_INTEGRATION=true to enable Google Calendar integration
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true to enable in-app notifications
NEXT_PUBLIC_ENABLE_ANALYTICS=false to disable analytics for privacy (recommended for development)

# Development Settings
NEXT_PUBLIC_DEBUG_MODE=true to enable additional logging and debugging features
NEXT_PUBLIC_MOCK_SERVICES=false to use real services instead of mocks

# Security Settings
NEXT_PUBLIC_ENCRYPTION_SALT=Random string used as salt for encryption (keep this secret)